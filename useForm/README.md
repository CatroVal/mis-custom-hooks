# useForm Hook

Ejemplo de uso:
```
    const initialValue = {
        name: '',
        age: 0,
        email: ''
    }
    
    const [formValues, handleInputChange, reset] = useForm(initialValue);

```